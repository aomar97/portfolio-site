apiVersion: v1
kind: Service
metadata:
  name: {{ include "site.name" . }}
  labels:
    {{- include "site.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "site.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 80
      targetPort: http
